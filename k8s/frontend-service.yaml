apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  type: ClusterIP   #  ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80         
      targetPort: 80   
